# Python Recursion & Math Module Programs

##  Overview

This repository contains Python programs related to **recursion**, **user input handling**, and the use of Python’s built-in **math module**.

The repository includes solutions for the following tasks:

---

##  Task 1: Calculate Factorial Using a Function

### 🔹 Objective
The objective of this task is to calculate the factorial of a given number by defining and using a function.  
The solution uses **recursion** to compute the factorial.

### 🔹 Functionality
- Defines a function named `factorial()`
- Accepts a number as an argument
- Calculates the factorial using **recursion**
- Returns the calculated factorial value
- Calls the function with a sample input and prints the result

### 🔹 Concepts Used
- Functions
- Recursion
- Conditional statements
- Return values

---

##  Task 2: Using the Math Module for Calculations

### 🔹 Objective
This task demonstrates the use of Python’s built-in `math` module to perform common mathematical operations on a user-provided number.

### 🔹 Functionality
- Takes a number as input from the user
- Uses the `math` module to calculate:
  - Square root of the number
  - Natural logarithm (base *e*)
  - Sine of the number (in radians)
- Displays the calculated results clearly

### 🔹 Concepts Used
- Importing modules
- `math.sqrt()`
- `math.log()`
- `math.sin()`
- User input and output formatting

---


